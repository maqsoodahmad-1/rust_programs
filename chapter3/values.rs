fn main()
{
   /*.Reserves an object(i.e,an area of memory)
     .Stores the value 12 in such object
     .Associates the name number to such object*/
    let number = 12;

    let another_number = 34;
    //uses the above decleared and initialized variables 
    println!("{}", number + another_number);
}
