fn main()
{
    let truth = 5 > 2;  //Genrating the value of truth from relational expression
    let falsity = -10 > 2;//Generating the value of falsity from relational expression
    println!("{} {} ",truth, falsity);
}
